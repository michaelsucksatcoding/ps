# Task TODO

- [x] Add `@main_bp.get('/')` in `grading_project/routes.py` to render a landing page or redirect to `/auth/login` when not authenticated.

- [x] Ensure templates do not contain legacy `.php` links; use `url_for(...)` or absolute paths like `/auth/login`.
- [x] Re-check templates and routes for remaining `.php`/relative auth links.
- [x] Attempt to verify registered routes include `/` and `/auth/login`.



